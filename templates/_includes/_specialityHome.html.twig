<section class="ca-speciality-container my-5">
    <div class="ca-background-speciality"><div></div></div>
    {% for special in speciality %}
        <article>
            <div class="ca-box-speciality">


                <div class="ca-box-speciality-hover c-dflex-columns justify-content-between py-3">
                    <a href="{{ path('speciality_index',{id : special.id}) }}" class="h4">{{ special.nameSpeciality }}</a>

                </div>
                <img src="{{asset('assets/uploads/pictures/mini/300x300-' ~ special.picture.name) }}" alt="">
            </div>
        </article>
    {% endfor %}
</section>