{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}
   <h1>modifier cette spécialité</h1>
    {{ include('speciality/_form.html.twig', {'button_label': 'Valider'}) }}

   {%  if speciality.picture is not null %}
        <a href="{{path('pictures_delete_speciality', {id: speciality.picture.id})}}">
            <img src="{{asset('assets/uploads/pictures/mini/300x300-' ~ speciality.picture.name) }}" alt="">
        </a>

{% endif %}
{% endblock %}