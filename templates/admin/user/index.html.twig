{% extends 'baseAdmin.html.twig' %}

{% block title %}Hello Admin{% endblock %}
{%  block CssHeader %}ca-user-display{%  endblock %}
{% block headerH1 %}<h1 class="h2 ca-h1-form mx-auto text-center pt-3 mt-2 mb-5">
    Tous les utilisateurs</h1>{% endblock %}
{% block headerImg %}

{% endblock %}

{% block headerPara %}

   {% endblock %}
{% block headerReservation %}{% endblock %}
{% block body %}
    <section class="ca-section">
        {% for user in users %}<li>N° id : {{ user.id }} <a href="{{ path('user_delete', { id: user.id}) }}">supprimez cet utilisateur</a></li>
            <ul class="ca-user-admin">

                <li>Nom : <a href="{{ path('admin_user_edit', {id : user.id}) }}">{{ user.fullname }}</a></li>
                <li>Email : <em>{{ user.email }}</em></li>
                <li>Role : <em>{{ user.roles|join(', ') }}</em></li>
            </ul>

        {% endfor %}
    </section>
{% endblock %}